# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1

# PDF / OCR / Excel
pdfplumber==0.11.4
pytesseract==0.3.13
opencv-python-headless==4.10.0.84
pandas==2.2.3
openpyxl==3.1.5
Pillow==11.0.0
numpy>=1.26.0,<2.3
pypdfium2==4.30.1

# AI extraction (optional at runtime if no API key)
langchain==0.3.14
langchain-openai==0.2.14
openai==1.58.1

# Optional table extraction — NOT required to run the app.
# camelot-py[cv] pulls pdftopng which often fails on Windows.
# Install manually only if you need Camelot:
#   pip install "camelot-py==0.11.0"
# and install Ghostscript from https://ghostscript.com/
