# SteelDraw AI Extractor — backend dependencies
# Use Python 3.10, 3.11, or 3.12 on Windows (recommended: 3.11 or 3.12).
# Python 3.13+ may need newer package versions (already allowed below).

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
python-dotenv==1.0.1
pdfplumber==0.11.4
pytesseract==0.3.13
# Headless OpenCV — use a version with Windows wheels
opencv-python-headless>=4.10.0.84,<4.13
# Flexible pins so pip can pick a prebuilt wheel (avoids "metadata-generation-failed")
numpy>=1.26.4,<2.3
pandas>=2.2.3,<2.3
openpyxl==3.1.5
langchain==0.3.14
langchain-openai==0.3.0
langchain-google-genai==2.0.8
openai==1.59.3
google-generativeai==0.8.4
pypdf>=5.1.0,<6
Pillow>=11.1.0,<12
aiofiles==24.1.0

# Optional — only if you have Ghostscript installed (often fails on Windows):
#   pip install "camelot-py==0.11.0" ghostscript
